// Web stub for expo-av — use HTML5 Audio via useAudioPlayer.web.ts instead
const noop = () => {};

const Audio = {
  setAudioModeAsync: async () => {},
  Sound: {
    createAsync: async () => ({ sound: null }),
  },
  Recording: {},
};

const Video = {};

module.exports = { Audio, Video };
